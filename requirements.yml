---
# requirements.yml — Install any external Ansible collections needed

collections:
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.mysql
    version: ">=3.0.0"
  - name: community.postgresql
    version: ">=3.0.0"
